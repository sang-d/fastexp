
name: Integration tests workflow

on:
  workflow_dispatch:
  push:
    paths-ignore:
      - 'static/**'
  pull_request:
    paths-ignore:
      - 'static/**'

jobs:
  integration-test:
    runs-on: ubuntu-latest
    steps:
    - run: 'sh fail.sh'
    - run: 'echo "Integration test done!!"'
